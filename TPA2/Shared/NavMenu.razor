
        <div class="top-row ps-3 navbar navbar-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="">TPA2</a>
                <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
        </div>

        <div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
            <nav class="flex-column">
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="page1" Match="NavLinkMatch.All">
                        <span class="oi oi-home" aria-hidden="true"></span> Public Page
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <AuthorizeView Roles="administrator">
                        <NavLink class="nav-link" href="page2">
                            <span class="oi oi-lock-locked" aria-hidden="true"></span> Administrator Page
                        </NavLink>
                    </AuthorizeView>
                </div>
            </nav>
        </div>
    


@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
